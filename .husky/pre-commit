echo "🔍 Running pre-commit checks..."

# Generate Nuxt types first (required for auto-imports)
echo "Generating Nuxt types..."
npx nuxi prepare || {
  echo "⚠️  Warning: Failed to generate Nuxt types"
  echo "💡 Attempting alternative method..."
  npm run build --dry-run 2>/dev/null || npx nuxt prepare || {
    echo "⚠️  Could not generate Nuxt types, skipping TypeScript check"
    echo "💡 Tip: Run 'npm run dev' once to generate Nuxt types"
  }
}

# Check if .nuxt/tsconfig.json exists (required for TypeScript to find auto-imports)
if [ -f ".nuxt/tsconfig.json" ]; then
  echo "Checking TypeScript..."
  npx tsc --noEmit --skipLibCheck || {
    echo "❌ TypeScript check failed"
    echo "💡 Tip: Run 'npm run dev' once to generate Nuxt types, then try again"
    exit 1
  }
else
  echo "⚠️  Skipping TypeScript check (.nuxt/tsconfig.json not found)"
  echo "💡 Tip: Run 'npm run dev' once to generate Nuxt types"
  echo "💡 This is normal if you haven't run the dev server yet"
fi

# Check Python syntax
echo "Checking Python syntax..."
python -m py_compile web-gui/server.py || {
  echo "❌ Python syntax check failed"
  exit 1
}

# Check for common issues
echo "Checking for common issues..."
if git diff --cached --name-only | grep -q "\.json$"; then
  echo "Validating JSON files..."
  git diff --cached --name-only | grep "\.json$" | while read file; do
    python -m json.tool "$file" > /dev/null || {
      echo "❌ Invalid JSON: $file"
      exit 1
    }
  done
fi

echo "✅ Pre-commit checks passed"
