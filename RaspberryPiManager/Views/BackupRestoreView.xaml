<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="RaspberryPiManager.Views.BackupRestoreView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:RaspberryPiManager.ViewModels"
    xmlns:models="clr-namespace:RaspberryPiManager.Models"
    x:DataType="vm:BackupRestoreViewModel"
    Title="Backup &amp; Restore">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">
        <StackLayout Grid.Row="0" Orientation="Horizontal" Spacing="10">
            <Button Text="Create Backup" Command="{Binding CreateBackupCommand}" />
            <Button Text="Refresh" Command="{Binding LoadBackupsCommand}" />
        </StackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Backups}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackLayout Grid.Column="0">
                            <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold" />
                            <Label Text="{Binding CreatedDate, StringFormat='Created: {0:g}'}" FontSize="12" />
                            <Label Text="{Binding BackupSizeBytes, StringFormat='Size: {0:N0} bytes'}" FontSize="12" />
                        </StackLayout>
                        <Button Grid.Column="1" Text="Restore"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RestoreBackupCommand}"
                                CommandParameter="{Binding .}" />
                        <Button Grid.Column="2" Text="Delete"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteBackupCommand}"
                                CommandParameter="{Binding .}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <StackLayout Grid.Row="2">
            <ProgressBar Progress="{Binding BackupProgress}" IsVisible="{Binding IsBackingUp}" />
            <ProgressBar Progress="{Binding RestoreProgress}" IsVisible="{Binding IsRestoring}" />
            <Label Text="{Binding StatusMessage}" />
        </StackLayout>
    </Grid>
</ContentPage>
